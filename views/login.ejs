<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Food Panda</title>
    <link rel="stylesheet" href="css/login.css">
</head>

<body>
    <div class="container">
        <div class="card">
            <h1 class="card-title">Welcome Back!</h1>
            <small class="card-subtitle">Enter your credentials to gain access</small>
            <form action="/login" method="POST" class="card-form">
                <label for="email">Email</label>
                <div class="card-input-container username">
                    <input type="email" placeholder="Enter your email" name="email" id="email">
                </div>
                <label for="password">Password</label>
                <div class="card-input-container password">
                    <input type="password" placeholder="Enter your password" name="password" id="password">
                </div>
                <button class="card-button">Log In</button>
                <small class="card-forgot-password">Don't Have an account? <a href="register">Register Here</a></small>
                <small class="card-forgot-password">Forgot your passwrod ? <a>Reset Password</a></small>

                <% if (messages.error) { %>
                  <%= messages.error %>
                <% } %>

            </form>
        </div>
    </div>
</body>

</html>
